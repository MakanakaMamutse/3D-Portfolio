// Import the SectionWrapper component from the SectionWrapper file
// This component is likely used to wrap sections of your app with consistent styling or functionality
import SectionWrapper from "./SectionWrapper";

// Export the SectionWrapper component to make it available for use in other parts of the app
// This allows other files to import and use SectionWrapper as needed
export { SectionWrapper };